#include<stdio.h>
int main(){
int i,j,res;
printf("Enter the number:");
scanf("%d",&i);
printf("Enter the number to multiple:");
scanf("%d",&j);

res=i*j;
printf("%dX%d = %d",i,j,res);

return 0;
}
